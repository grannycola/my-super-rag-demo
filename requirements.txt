# CPU-only stack (VPS / local dev without GPU).
# PyTorch CPU wheel — must stay before sentence-transformers.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.32.0
numpy>=1.26.0,<2.4.0
chromadb>=0.5.0
qdrant-client>=1.12.0
openai>=1.50.0
sentence-transformers>=3.0.0
httpx>=0.27.0
streamlit>=1.40.0
pytest>=8.0.0
